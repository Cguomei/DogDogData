# 项目开发文档（重构版）

## 📋 目录

1. [项目概述](#项目概述)
2. [技术架构](#技术架构)
3. [代码规范](#代码规范)
4. [目录结构](#目录结构)
5. [数据库设计](#数据库设计)
6. [API 接口规范](#api 接口规范)
7. [开发环境配置](#开发环境配置)
8. [部署指南](#部署指南)

---

## 项目概述

狗狗数据分析系统是一个基于 Flask 的数据可视化平台，提供狗狗品种管理、数据统计分析、图表展示等功能。

### 核心功能
- ✅ 用户认证系统（Session + JWT Token）
- ✅ 数据看板（8 项核心指标）
- ✅ 6 种数据图表展示
- ✅ 狗狗品种 CRUD 管理
- ✅ 用户收藏功能
- ✅ APP 接口支持（预留）

---

## 技术架构

### 后端技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Python | 3.8+ | 开发语言 |
| Flask | 3.1.3 | Web 框架 |
| SQLAlchemy | 2.0.48 | ORM 框架 |
| PyMySQL | 1.1.2 | MySQL 驱动 |
| Flask-Login | 0.6.3 | 用户认证 |
| Flask-Caching | 2.3.1 | 缓存管理 |
| Flask-APScheduler | 1.13.1 | 定时任务 |
| PyJWT | 2.10.1 | JWT Token（APP 用） |
| PyECharts | 2.1.0 | 数据可视化 |
| Pandas | 2.3.3 | 数据处理 |

### 数据库
- MySQL 8.0+
- 字符集：utf8mb4
- 存储引擎：InnoDB

---

## 代码规范

### 1. 项目结构

```
fastApiProject/
├── api/                      # API 蓝图模块
│   ├── __init__.py
│   └── routes.py            # API 路由定义
├── utils/                    # 工具类
│   ├── __init__.py
│   ├── api_response.py      # API 响应助手
│   └── auth.py              # 认证装饰器
├── templates/                # HTML 模板
├── static/                   # 静态资源
├── config.py                 # 配置文件
├── models.py                 # 基础数据模型
├── models_extended.py        # 扩展数据模型
├── charts.py                 # 图表生成
├── map_utils.py              # 地图工具
├── errors.py                 # 错误处理
├── init_db.py                # 数据库初始化脚本
├── app.py                    # 主应用入口
└── requirements.txt          # 依赖包
```

### 2. 代码组织原则

#### 模块化设计
- **单一职责**：每个模块只负责一个功能领域
- **高内聚低耦合**：模块间通过明确定义的接口通信
- **可测试性**：模块应易于单元测试

#### 命名规范
```python
# 文件命名：小写 + 下划线
config.py
models_extended.py
api_response.py

# 类命名：大驼峰
class APIResponse:
    pass

# 函数命名：小写 + 下划线
def get_breeds():
    pass

# 常量：全大写 + 下划线
MAX_PAGE_SIZE = 100
```

### 3. API 响应格式

所有 API 统一使用标准响应格式：

#### 成功响应
```json
{
  "code": 200,
  "success": true,
  "message": "操作成功",
  "data": { ... },
  "timestamp": 1679548800
}
```

#### 错误响应
```json
{
  "code": 400,
  "success": false,
  "message": "错误描述",
  "error_code": "VALIDATION_ERROR",
  "data": null,
  "timestamp": 1679548800
}
```

#### 分页响应
```json
{
  "code": 200,
  "success": true,
  "message": "获取成功",
  "data": [...],
  "meta": {
    "total": 100,
    "page": 1,
    "per_page": 20,
    "pages": 5,
    "has_next": true,
    "has_prev": false
  },
  "timestamp": 1679548800
}
```

---

## 数据库设计

### 核心表结构

#### 1. users - 用户表
```sql
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(80) UNIQUE NOT NULL,
    password_hash VARCHAR(200) NOT NULL,
    role VARCHAR(20) DEFAULT 'user',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 2. dog_breeds - 狗狗品种表
```sql
CREATE TABLE dog_breeds (
    id INT AUTO_INCREMENT PRIMARY KEY,
    breed_name VARCHAR(100) UNIQUE NOT NULL,
    avg_life_years DECIMAL(3,1),
    size_category ENUM('小型', '中型', '大型', '超大型'),
    popularity INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 3. user_profiles - 用户资料扩展表
```sql
CREATE TABLE user_profiles (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT UNIQUE NOT NULL,
    nickname VARCHAR(80),
    avatar_url VARCHAR(255),
    gender ENUM('男', '女', '保密'),
    phone VARCHAR(20),
    email VARCHAR(120),
    bio TEXT,
    location VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

#### 4. app_tokens - APP Token 表（为 APP 预留）
```sql
CREATE TABLE app_tokens (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    access_token VARCHAR(500) UNIQUE NOT NULL,
    refresh_token VARCHAR(500) UNIQUE NOT NULL,
    device_id VARCHAR(100),
    device_name VARCHAR(100),
    app_version VARCHAR(20),
    platform VARCHAR(20),
    expires_at DATETIME NOT NULL,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_used_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    INDEX idx_access_token (access_token),
    INDEX idx_refresh_token (refresh_token)
);
```

#### 5. user_favorites - 用户收藏表
```sql
CREATE TABLE user_favorites (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    breed_id INT NOT NULL,
    note VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY unique_user_breed (user_id, breed_id),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (breed_id) REFERENCES dog_breeds(id) ON DELETE CASCADE
);
```

---

## API 接口规范

### RESTful API 设计

#### 资源命名
- 使用复数名词：`/api/v1/breeds`
- 嵌套资源：`/api/v1/users/{id}/favorites`

#### HTTP 方法
| 方法 | 用途 | 幂等性 |
|------|------|--------|
| GET | 获取资源 | 是 |
| POST | 创建资源 | 否 |
| PUT | 更新资源（全量） | 是 |
| PATCH | 更新资源（部分） | 否 |
| DELETE | 删除资源 | 是 |

### 认证方式

#### 1. Session 认证（Web 端）
```python
from flask_login import login_required

@app.route('/protected')
@login_required
def protected():
    return jsonify({'message': 'Success'})
```

#### 2. JWT Token 认证（APP 端）
```python
from utils.auth import token_required

@api_bp.route('/protected')
@token_required
def protected():
    return APIResponse.success({'message': 'Success'})
```

#### 3. 混合认证
```python
from utils.auth import login_required_api

@api_bp.route('/protected')
@login_required_api  # 优先 Token，其次 Session
def protected():
    return APIResponse.success({'message': 'Success'})
```

### 接口列表

#### 公共接口（无需认证）
- `GET /api/v1/breeds` - 获取品种列表
- `GET /api/v1/breeds/{id}` - 获取品种详情

#### 需要认证的接口
- `POST /api/v1/breeds` - 添加品种
- `PUT /api/v1/breeds/{id}` - 更新品种
- `DELETE /api/v1/breeds/{id}` - 删除品种
- `GET /api/v1/profile` - 获取用户资料
- `GET /api/v1/favorites` - 获取收藏列表
- `POST /api/v1/favorites/{breed_id}` - 添加收藏
- `DELETE /api/v1/favorites/{breed_id}` - 取消收藏

---

## 开发环境配置

### 1. 环境要求
- Python 3.8+
- MySQL 8.0+
- Git

### 2. 安装步骤

```bash
# 克隆项目
git clone <repository-url>
cd fastApiProject

# 创建虚拟环境
python -m venv venv
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，配置数据库连接

# 初始化数据库
python init_db.py

# 启动应用
python app.py
```

### 3. 环境变量配置

`.env` 文件：
```env
# Flask 配置
FLASK_ENV=development
SECRET_KEY=your-secret-key-here

# 数据库配置
DB_HOST=localhost
DB_USER=doguser
DB_PASSWORD=123456
DB_NAME=dog

# JWT 配置
JWT_SECRET_KEY=jwt-secret-key-change-in-production

# 日志配置
LOG_LEVEL=DEBUG
LOG_FILE=logs/app.log
```

---

## 部署指南

### 生产环境配置

#### 1. 修改配置
```python
# config.py
class ProductionConfig(Config):
    DEBUG = False
    SESSION_COOKIE_SECURE = True  # HTTPS only
    LOG_LEVEL = 'WARNING'
```

#### 2. 使用 Gunicorn 部署
```bash
# 安装 Gunicorn
pip install gunicorn

# 启动
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

#### 3. Nginx 反向代理配置
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 测试

### 运行测试
```bash
# 运行所有测试
pytest

# 运行特定测试
pytest tests/test_api.py

# 查看覆盖率
pytest --cov=app tests/
```

---

## 常见问题

### Q1: 数据库连接失败
检查 `.env` 文件中的数据库配置是否正确，确保 MySQL 服务已启动。

### Q2: CSRF Token 错误
确保表单中包含 CSRF Token：
```html
<form method="POST">
    {{ form.hidden_tag() }}
    <!-- 其他字段 -->
</form>
```

### Q3: JWT Token 过期
Token 默认 1 小时过期，使用 Refresh Token 刷新：
```python
from utils.auth import refresh_access_token

new_token = refresh_access_token(refresh_token)
```

---

## 更新日志

### v2.0 (重构版) - 2026-03-23
- ✨ 重构项目结构，采用模块化设计
- ✨ 新增 API Blueprint，统一管理 API 路由
- ✨ 新增 JWT Token 支持（为 APP 预留）
- ✨ 新增用户收藏功能
- ✨ 新增扩展数据模型（UserProfile, AppToken 等）
- 🔧 优化错误处理机制
- 🔧 统一 API 响应格式
- 📝 完善开发文档

### v1.0 (初始版)
- ✨ 基础用户认证系统
- ✨ 数据看板展示
- ✨ 6 种图表类型
- ✨ 品种管理功能

---

## 联系方式

如有问题，请联系开发团队或提交 Issue。
